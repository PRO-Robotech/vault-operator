apiVersion: vault.in-cloud.io/v1alpha1
kind: VaultClaim
metadata:
  labels:
    app.kubernetes.io/name: vault-operator
    app.kubernetes.io/managed-by: kustomize
  name: vaultclaim-sample
spec:
  # TODO(user): Add fields here
